@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>13.05 изменила верстку у вкладок с постами из инсты. с компа теперь читается сверху-вниз.</p>

<p>You observe next <b>@ViewBag.users.Length</b> instagram accounts:</p>
<b>
    @{
        foreach (string user in @ViewBag.users)
        {
            @user
            <br />
        }
    }
</b>
<br />

You can add/delete insagram user. Put username below.
<div>
    @using (Ajax.BeginForm("AjaxUpdate", new AjaxOptions
    { UpdateTargetId = "update" }))
    {
        string user = "";
        <p> Username : </p>    @Html.EditorFor(x => user)
        <input type="submit" value="Submit" />
    }

<div id="update">


</div>
</div>
@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/ajax")



